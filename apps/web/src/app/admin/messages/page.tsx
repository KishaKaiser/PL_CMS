import { PrivateMessageInbox } from '../../../components/private-message-inbox';

export default function AdminMessagesPage() {
  return (
    <PrivateMessageInbox
      title="Private Messages"
      description="Send direct messages to users from the admin area."
    />
  );
}
